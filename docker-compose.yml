version: "3.8"

services:
  lantern:
    build: .
    ports:
      - "8080:8000"
    volumes:
      - ./data:/app/data   # Persists the SQLite database across container restarts
    restart: unless-stopped