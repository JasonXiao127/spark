version: "3.8"

services:
  lantern:
    build: .
    ports:
      - "8282:8282"
    volumes:
      - ./data:/app/data   # Persists the SQLite database across container restarts
    restart: unless-stopped